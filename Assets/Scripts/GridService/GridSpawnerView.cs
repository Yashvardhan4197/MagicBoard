
using UnityEngine;

public class GridSpawnerView : MonoBehaviour
{
    private GridSpawnerController controller;
    private void Start()
    {
        
    }

    public void SetController(GridSpawnerController controller)=>this.controller = controller;

}
